<%= form_for(@comment) do |f| %>

  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>

  <div class="actions">
    <%= f.submit "Edit", class: "medium button"%>
  </div>
<% end %>
